{
	"name": "webrtc-server",
	"version": "0.0.1",
	"scripts": {
		"start": "node server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
    "ws": "^3.2.1"
	},
	"devDependencies": {
		"@types/ws": "^3.2.1"
	},
	"type": "module"
}
